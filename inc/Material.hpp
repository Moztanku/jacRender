/**
 * @file Material.hpp
 * @brief Header file for the Material class, which represents a material with its properties and textures
 */
#pragma once

#include <assimp/material.h>

class Material {
public:
    // Material(const aiMaterial* material, const aiScene* scene)
    // {

    // }
private:
};
